#include<stdio.h>

int main(){
  int a; //variable declaration you have to always declare in c
  a = 6; // intialization 
  printf("the output of the program is %d", a);// by using %d we are using it in place of a because we can't directly write it
return 0;
}